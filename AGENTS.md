# AGENTS.md

## Cursor Cloud specific instructions

This repository (`ai-code`) is currently an empty scaffold with only a `README.md`. There is no application code, no package manager lockfile, no build system, and no services to run.

When code is added in the future, update this section with:
- How to install dependencies (e.g. `npm install`, `pip install -r requirements.txt`)
- How to run lint, tests, and the dev server
- Any non-obvious caveats discovered during setup
